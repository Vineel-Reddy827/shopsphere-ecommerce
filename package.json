{
  "name": "task-6-product-catalog",
  "version": "1.0.0",
  "description": "Standalone Full Stack ecommerce Product Catalog — Task 6 (Vite + Vanilla JS + JSON Server)",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "dev:api": "json-server --watch server/db.json --port 3001",
    "dev:all": "concurrently -k -n API,WEB \"npm run dev:api\" \"npm run dev\"",
    "build": "vite build",
    "preview": "vite preview --port 5173"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^9.1.2",
    "json-server": "^0.17.4",
    "vite": "^6.3.5"
  }
}
